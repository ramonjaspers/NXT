#include <stdio.h>
#include <iostream>
#include "state.h"

/**
 *
 * @param [in] argc  (argument count)will be the number of strings pointed to by argv
 * @param [in] argv  (argument vector)
 * @return [out] 0 or NULL
 */

int main(int argc, char **argv) {

    // new state object takes it all away
    State st;
    std::cout << 0 << std::endl;

    return 0;
}
